server:
  port:
    7070

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/(db 스키마)?serverTimezone=Asia/Seoul
    username: (아이디)
    password: (패스워드)